name: gym_tracker_flutter
description: "A Neo-Dark Bento Edition Gym Tracker migrated from React Native."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  lucide_icons_flutter: ^1.0.0
  fl_chart: ^0.66.0
  vibration: ^2.0.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
